<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.micheal.micheal_application">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- 欢迎页 -->
        <activity android:name=".Activity.GuideView.WelcomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 引导页 -->
        <activity android:name=".Activity.GuideView.IntroPage" >

        </activity>
        <!-- 主Activity -->
        <activity android:name=".Activity.MainActivity" />
        <!-- 百度地图页面 -->
        <activity android:name=".Activity.Baidu_Activity" />
        <!-- 城市选择页面 -->
        <activity android:name=".Fragment.Message.ui.SelectCity"/>

        <!-- 百度地图Key -->
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="5CkWT0aUHHQtGE9cWT2ePjY6bQwwcHdT" />
        <!-- 音乐播放器页面 -->
        <activity android:name=".Fragment.Home.ui.MediaActivity" />
        <!-- 音乐播放器服务 -->
        <service android:name=".Fragment.Home.utils.MediaServer" />

    </application>

</manifest>
